package com.atball.der.search.proxy;

public class Proxy {

    // java 实现代理的两种办法
    // 代理的名词
    /**
     * 代理对象     增强后的对象
     * 目标对象     被增强的对象
     * 他们不是绝对的，会根据情况发生变化
     *
     * 静态代理：
     *      继承：
     *          代理对象继承目标对象，重写需要增强的方法
     *          缺点：代理类会过多，非常复杂
     *      聚合：
     *          目标对象和代理对象实现同一个接口，代理对象当中要包含目标对象。
     *
     * 动态代理：
     *
     */
}
